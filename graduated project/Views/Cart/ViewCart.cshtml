@model List<Product>

<h2>Your Cart</h2>

@if (!Model.Any())
{
    <p>Your cart is empty.</p>
}
else
{
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Name - @product.Priceafterdiscount جنيه</li>
        }
    </ul>
}
